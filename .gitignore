__pycache__/

# Local datasets and bulky regenerable render outputs stay untracked;
# experiment result CSVs are tracked (user decision, 2026-07-16).
/data/PASLCD
/output_subset/**
!/output_subset/.gitkeep

# Notion API key (local only, never commit)
.notion_token
