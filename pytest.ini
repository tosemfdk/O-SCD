[pytest]
markers =
    cuda: requires CUDA and the compiled Gaussian rasterizer
    mcmc: MCMC fixed-topology temporal R_change tests
    integration: integration tests that may cross module or artifact boundaries
